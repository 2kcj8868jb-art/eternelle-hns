<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Eternelle HNS</title>

  <!-- FONTS -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600&family=Inter:wght@300;400&display=swap" rel="stylesheet">

  <style>
    /* ================= RESET ================= */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: #f6f2ee;
      color: #2a1b13;
      overflow-x: hidden;
    }

    /* ================= NAV ================= */
    nav {
      position: fixed;
      top: 0;
      width: 100%;
      padding: 28px 90px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      z-index: 100;
      background: transparent;
      transition: background 0.4s ease;
    }

    nav.scrolled {
      background: #2a1b13;
    }

    .logo {
      font-family: 'Playfair Display', serif;
      font-size: 22px;
      color: #ffffff;
      letter-spacing: 0.5px;
    }

    nav ul {
      display: flex;
      list-style: none;
      gap: 36px;
      color: #ffffff;
      font-size: 14px;
    }

    nav li {
      cursor: default;
    }

    /* ================= HERO ================= */
    .hero {
      height: 100vh;
      background: linear-gradient(
        180deg,
        #3a2216 0%,
        #6b4029 45%,
        #f6f2ee 100%
      );
      display: flex;
      align-items: center;
    }

    .hero-inner {
      padding: 0 90px;
      max-width: 750px;
    }

    .hero h1 {
      font-family: 'Playfair Display', serif;
      font-size: 68px;
      font-weight: 400;
      color: #ffffff;
      margin-bottom: 26px;
      line-height: 1.1;
    }

    .hero p {
      font-size: 16px;
      color: #efe4dc;
      max-width: 520px;
    }

    /* ================= SECTIONS ================= */
    section {
      padding: 160px 90px;
      text-align: center;
    }

    .light {
      background: #ffffff;
    }

    .dark {
      background: #2a1b13;
      color: #f6f2ee;
    }

    .quote {
      font-family: 'Playfair Display', serif;
      font-size: 44px;
      font-weight: 400;
      line-height: 1.3;
    }

    .statement {
      max-width: 620px;
      margin: auto;
      font-size: 16px;
      line-height: 1.9;
    }

    /* ================= TYPO ANIMATION ================= */
    .fade {
      opacity: 0;
      transform: translateY(50px);
      transition: all 1.2s ease;
    }

    .fade.show {
      opacity: 1;
      transform: translateY(0);
    }

    /* ================= DIVIDER ================= */
    .divider {
      width: 1px;
      height: 120px;
      background: #cbb9ae;
      margin: 120px auto;
    }

    /* ================= 2 BILDER NEBENEINANDER ================= */
    .image-double {
      display: flex;
      justify-content: center;
      gap: 60px;
      padding: 120px 90px;
      align-items: center;
    }

    .image-double img {
      width: 40%;
      max-width: 500px;
      object-fit: cover;
      border-radius: 4px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    }

    .image-quote {
      text-align: center;
      font-family: 'Playfair Display', serif;
      font-size: 24px;
      margin-top: 40px;
      margin-bottom: 140px;
      color: #2a1b13;
    }

    /* ================= FOOTER ================= */
    footer {
      padding: 120px 90px;
      background: #2a1b13;
      color: #f6f2ee;
      text-align: center;
      font-size: 14px;
      line-height: 2;
    }

    footer span {
      font-family: 'Playfair Display', serif;
      font-size: 18px;
      display: block;
      margin-bottom: 20px;
    }

    /* ================= MOBILE ================= */
    @media (max-width: 768px) {
      nav,
      .hero-inner,
      section,
      footer {
        padding: 80px 32px;
      }

      .hero h1 {
        font-size: 44px;
      }

      .quote {
        font-size: 28px;
      }

      nav ul {
        display: none;
      }

      .image-double {
        flex-direction: column;
        gap: 40px;
        padding: 80px 30px;
      }

      .image-double img {
        width: 100%;
      }

      .image-quote {
        font-size: 20px;
      }
    }
  </style>
</head>

<body>

  <!-- NAV -->
  <nav id="nav">
    <div class="logo">Eternelle HNS</div>
    <ul>
      <li>Home</li>
      <li>Philosophie</li>
      <li>Kollektion</li>
      <li>Kontakt</li>
    </ul>
  </nav>

  <!-- HERO -->
  <header class="hero">
    <div class="hero-inner fade">
      <h1>Designed for now,<br>made for later.</h1>
      <p>
        Nachhaltige Eleganz aus Second-Hand-Materialien.
        Reduziert im Design, klar in der Haltung.
      </p>
    </div>
  </header>

  <!-- QUOTE 1 -->
  <section class="fade">
    <p class="quote">
      „Zeitlosigkeit beginnt dort,<br>
      wo Trends enden.“
    </p>
  </section>

  <div class="divider"></div>

  <!-- STATEMENT -->
  <section class="light fade">
    <p class="statement">
      Eternelle HNS ist ein konzeptionelles Taschenprojekt,
      das Nachhaltigkeit und High-End-Ästhetik vereint.
      Die Website verzichtet bewusst auf Bilder und lenkt
      den Fokus auf Typografie, Raum und Haltung.
    </p>
  </section>

  <!-- QUOTE 2 -->
  <section class="dark fade">
    <p class="quote">
      „Weniger, aber besser.“
    </p>
  </section>

  <!-- 2 BILDER NEBENEINANDER -->
  <section class="image-double fade">
    <img src="bag-large.jpg" alt="Eternelle HNS Large Tasche">
    <img src="bag-mini.jpg" alt="Eternelle HNS Mini Tasche">
  </section>

  <p class="image-quote fade">Designed for now, made for later.</p>

  <div class="divider"></div>

  <!-- QUOTE 3 -->
  <section class="fade">
    <p class="quote">
      „Designed to adapt.<br>
      Created to last.“
    </p>
  </section>

  <!-- FOOTER -->
  <footer class="fade">
    <span>Eternelle HNS</span>
    NGN Nürnberg<br>
    Schulprojekt – nicht kommerziell
  </footer>

  <!-- ================= SCRIPT ================= -->
  <script>
    // NAV BACKGROUND ON SCROLL
    const nav = document.getElementById("nav");
    window.addEventListener("scroll", () => {
      if (window.scrollY > 80) {
        nav.classList.add("scrolled");
      } else {
        nav.classList.remove("scrolled");
      }
    });

    // FADE-IN ON SCROLL
    const elements = document.querySelectorAll(".fade");
    const observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add("show");
        }
      });
    }, { threshold: 0.15 });

    elements.forEach(el => observer.observe(el));
  </script>

</body>
</html>
